{% extends 'base.html.twig' %}

{% block body %}

<h3>Page d'ajout d'un plat</h3>

{{form_start(platForm)}}

<button type="submit" class="btn btn-success">Save</button>

{{form_end(platForm)}}

{% endblock %}
